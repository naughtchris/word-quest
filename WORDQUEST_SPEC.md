# WordQuest 純前端單字複習網站規格

版本：1.2  
更新日期：2026-08-14

教材欄位、圖片 OCR 轉檔與品質驗證的統一資料契約，請依 `vocabulary/VOCABULARY_CSV_RULES.md` 執行。

## 1. 產品目標

WordQuest 是不區分孩子、不需登入的繁體中文單字複習網站。使用者可單選或複選教材，依每份教材分別指定學期、單元與課次，再使用單字列表、閃卡及測驗複習。

- 網站為純靜態 HTML、CSS、JavaScript，不使用後端或資料庫。
- 單字資料隨網站發布，不在執行時連接外部資料來源。
- 學習紀錄只保存在目前瀏覽器，不提供帳號或跨裝置同步。
- 成品位於 `docs/`，可在本機預覽，亦可由 GitHub Pages 的 `/docs` 目錄發布。

## 2. 教材與學習範圍

### 2.1 教材選擇

- 首頁以教材卡片列出所有已匯入 CSV、單字數與學期。
- 使用者至少選一份教材，可同時選擇多份教材混合複習。
- 選擇教材後，網站提供單字複習、閃卡練習與單字測驗三個分頁。

### 2.2 每教材獨立篩選

每份選取的教材都有獨立的學期、單元及 Class 複選條件：

- 同一欄位內採聯集，例如選擇 U1、U2 代表 U1 或 U2。
- 不同欄位間採交集，例如第一學期、U1、Class 3 必須同時符合。
- 多份教材的篩選結果最後採聯集合併。
- 某欄位沒有選取任何項目時，代表該欄位不限制。
- 各教材顯示符合筆數，頁首顯示所有教材的合併筆數。
- 零筆結果必須明確提示，且不能開始閃卡或測驗。

範圍狀態概念如下：

```text
Record<bookId, {
  semesters: string[];
  units: string[];
  classes: string[];
}>
```

## 3. 學習模式

閃卡及測驗採「設定」與「專注工作階段」兩階段流程。開始工作階段後以 CSS 全視窗介面呈現，只保留離開、模式名稱、進度、更多選單、主要學習內容及必要操作；教材標題、分頁、篩選與頁尾全部隱藏。篩選只能在開始前調整。

### 3.1 單字複習

- 顯示英文、中文、詞性、類型、教材、學期、單元、Class 與頁碼。
- 增強教材另顯示動詞三態、縮寫、詞語關係、拼字變化、已確認例句及同概念筆數。
- `NeedsReview=Yes` 的內容仍可瀏覽與使用閃卡，但明確標示且不納入測驗。
- 支援中英文搜尋，以及全部、已熟悉、待加強篩選。
- 支援手動播放英文發音、標記已熟悉及標記待加強。

### 3.2 閃卡

- 開始前先確認範圍、英文／中文正面、依序／隨機，以及繼續上次或重新開始。
- 正面只顯示出題文字；翻面顯示另一種語言、詞性、結構資訊與一個例句，教材位置收在預設關閉的「更多內容」。
- 支援依序、隨機、上一張、下一張及只練待加強。
- 專注畫面底部固定提供上一張、待加強、發音、已熟悉與下一張五項操作。
- 鍵盤支援空白鍵翻面、左右鍵換卡、`1` 待加強、`2` 已熟悉、`R` 重播及 `Esc` 離開。
- 保存每份教材與篩選範圍的閃卡工作階段，包含卡片順序、目前索引、正反面與英文／中文出題設定。
- 重新整理或重新進入相同教材與篩選範圍時，顯示「已恢復進度」並回到上次位置；換教材或換範圍不套用舊工作階段。
- 英文為正面時，新卡出現後自動播放一次英文。
- 中文為正面時不先播放；翻到英文面後才自動播放，避免洩漏答案。
- 同一張卡因畫面重繪時不重播；換卡、重新排列或真正翻面時才依規則播放。
- 保留手動重播按鈕。瀏覽器不支援語音時隱藏發音控制，其餘功能照常使用。
- 發音一律使用增強欄位 `AudioText`；空白時不顯示播放按鈕，也不建立聽力題。

### 3.3 測驗

- 開始前顯示範圍摘要、可測驗筆數、題數、發音題開關及可用題型；開始後移除所有設定資訊。
- 題數可選 10 題、20 題或目前範圍全部；題目不足時使用所有可用內容。
- 題型完全由每筆資料的 `QuizModes` 與結構欄位決定，不得依教材名稱寫死。
- 基礎題型包含英翻中選擇、中翻英選擇、看中文拼英文。
- 欄位完整時自動加入縮寫互換、動詞三態、同義／反義、單複數拼字變化、文法辨義及句型填空。
- 測驗設定提供「加入發音題」開關，預設開啟。
- 開啟後再混入聽音選字與英文聽寫；每道發音題出現時自動播放一次，並提供手動重播。
- 關閉後只使用目前教材可用的文字題。瀏覽器不支援語音或範圍內沒有 `AudioText` 時，開關停用並顯示說明。
- 作答時只顯示題目進度，不顯示即時總分；教材來源在作答後才顯示。
- 作答後立即顯示精簡正確答案與必要的發音重播；完整統計、錯題及錯題重練集中於結果頁。
- 已開始作答時離開需確認，本次測驗題目進度不跨重新整理恢復。
- 拼字判定忽略英文大小寫與首尾空白，其餘字元必須相同。

## 4. 發音與本機紀錄

- 發音使用瀏覽器 Web Speech API，語言指定為 `en-US`，不下載或儲存真人音檔。
- 每次自動播放前取消尚未完成的上一段語音，避免重疊。
- `localStorage` 使用版本化鍵值保存每筆單字的熟悉狀態、答對／答錯次數、最後練習時間及最近一次測驗摘要。
- 同一個版本化紀錄另保存 `flashcardSessions` 與 `flashcardLastContext`，只保存在目前瀏覽器，不同步至雲端。
- 單字 ID 優先使用增強教材的 `VocabularyId`；備援 ID 由教材與必要欄位內容產生，不使用 CSV 列序。
- 舊版列序 ID 會在目前瀏覽器內自動遷移至穩定 ID，新增或重新排序教材不得改變既有學習紀錄。
- 清除所有學習紀錄必須連續確認兩次。

## 5. CSV 資料契約

### 5.1 檔案規則

- 正式教材放在 `vocabulary/enhanced/`，檔名必須符合 `*_vocabulary_enhanced.csv`；一份 CSV 即一份教材。
- 產生器掃描所有增強 CSV，新教材不需修改網站程式。
- `way_to_go_11_vocabulary_enhanced.csv` 會顯示為 `Way to Go 11`。
- 新增 CSV 後執行 `node scripts/generate-data.mjs`，產生 `docs/vocabulary-data.js`。

### 5.2 必要欄位

| 欄位 | 用途 |
| --- | --- |
| `English` | 英文單字或片語 |
| `Chinese` | 中文解釋 |
| `PartOfSpeech` | 詞性 |
| `type` | word、phrase 或其他教材類型 |
| `Class` | 課次，例如 `Class 3` |
| `Semester` | 學期，例如 `First Semester` |
| `Unit` | 單元，例如 `U1` 或 `Review` |
| `Page` | 教材頁碼 |

所有必要欄位必須存在且每列不得留空。CSV 解析必須支援 UTF-8、引號、逗號與中英文標點。同字異義或出現在不同課次的資料不得合併。

### 5.3 新教材流程

1. 依 `vocabulary/VOCABULARY_CSV_RULES.md` 產生增強 CSV。
2. 將增強檔放入 `vocabulary/enhanced/`。
3. 執行 `node scripts/generate-data.mjs`。
4. 確認輸出的教材數、總筆數、唯一 ID 及各題型可用數量。
5. 在本機預覽教材卡片、獨立篩選、閃卡與測驗。
6. 提交 CSV 與更新後的 `docs/vocabulary-data.js`。

## 6. 驗收條件

- 目前 Way to Go 1 為 149 筆、Way to Go 11 為 379 筆，合計 528 個唯一 ID。
- 不同教材可同時使用不同的學期、單元與 Class 條件，三種學習模式顯示相同範圍筆數。
- 英文正面閃卡換卡會發音，中文正面只在翻面後發音，且不因一般重繪重播。
- 發音題開關可正確加入或排除聽音選字、聽寫；自動與手動重播皆可運作。
- 無語音支援時網站仍可完成所有文字模式。
- 重新整理後本機學習紀錄仍存在；新增教材後既有紀錄不受影響。
- 手機寬度不產生水平捲動，所有按鈕可用鍵盤操作並具有可辨識標籤。
- 網站可由靜態伺服器或 GitHub Pages `/docs` 目錄載入，不需要建置服務或後端。
